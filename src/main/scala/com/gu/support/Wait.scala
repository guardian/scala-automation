package com.gu.support

import org.openqa.selenium.{WebDriver, WebElement}

/**
 * Created by ipamer on 17/06/2014.
 */
object Wait {

  def waitForElementPresent(element: WebElement)(implicit driver: WebDriver): Boolean = {
//    val driverWait = new WebDriverWait(driver, 30)
//    driverWait.wait(...)
    false
  }

}
